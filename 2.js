// Generated by CoffeeScript 1.3.3
(function() {
  var MAGIC_TEXT_LENGTH, getLongElements, getTextLength;

  MAGIC_TEXT_LENGTH = 30;

  getTextLength = function(elem) {
    var node, totalLength, _i, _len, _ref;
    totalLength = 0;
    _ref = elem.childNodes;
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      node = _ref[_i];
      if (node.nodeType === Node.TEXT_NODE) {
        totalLength += node.textContent.length;
      }
    }
    return totalLength;
  };

  getLongElements = function() {
    var longElements, p, paragraphs, _i, _len;
    longElements = [];
    paragraphs = document.getElementsByTagName("p");
    for (_i = 0, _len = paragraphs.length; _i < _len; _i++) {
      p = paragraphs[_i];
      if (getTextLength(p) > MAGIC_TEXT_LENGTH) {
        longElements.push(p);
      }
    }
    console.log(longElements);
    return longElements;
  };

  getLongElements();

}).call(this);
